apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-app-config
  namespace: production
data:
  APP_ENV:         "production"
  LOG_LEVEL:       "INFO"
  SERVER_PORT:     "8080"
  # Add more non-sensitive config here
  # For secrets, use Kubernetes Secrets or AWS Secrets Manager
